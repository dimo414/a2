# `a2` - supercharged worktrees

`a2` provides a higher-level abstraction on top of [Git Worktrees](https://git-scm.com/docs/git-worktree) to simplify developing with multiple checkouts of a single repository.
